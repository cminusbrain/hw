#include <iostream>

int main()
{
    int n;
    std::cin >> n;
    int first_digit = n / 100;
    int middle_digit = (n % 100) / 10;
    int last_digit = n % 10;
    std::cout << middle_digit << first_digit << last_digit;
    return 0;
}
