#include <iostream>

int main()
{
    int n;
    std::cin >> n;
    int first_digit = n / 10;
    int last_digit = n % 10;
    int sum = first_digit + last_digit;
    std::cout << sum;
    return 0;
}
